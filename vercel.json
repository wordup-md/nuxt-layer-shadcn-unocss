{
  "buildCommand": "pnpm build",
  "outputDirectory": ".playground/.vercel/output",
  "installCommand": "pnpm install",
  "ignoreCommand": "echo \"REF: $VERCEL_GIT_COMMIT_REF\" && echo \"MSG: $VERCEL_GIT_COMMIT_MESSAGE\" && git fetch --tags && ! git tag --points-at HEAD | grep -q \"^v\""
}

